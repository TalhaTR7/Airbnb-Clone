import './index.css'

export default function Section({ heading }) {
    return (
        <div className="section">
            <h1>{heading}</h1>
            <h1>{heading}</h1>
            <h1>{heading}</h1>
            <h1>{heading}</h1>
        </div>
    );
}
